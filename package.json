{
  "name": "doc-intel-agent",
  "private": true,
  "description": "Document Intelligence Agent - messy invoice extraction. Convenience scripts that shell out to backend/ and frontend/.",
  "scripts": {
    "backend:install": "npm install --prefix backend",
    "frontend:install": "npm install --prefix frontend",
    "install:all": "npm run backend:install && npm run frontend:install",
    "backend:dev": "npm run dev --prefix backend",
    "frontend:dev": "npm run dev --prefix frontend",
    "backend:test": "npm test --prefix backend",
    "frontend:test": "npm test --prefix frontend",
    "test:all": "npm run backend:test && npm run frontend:test",
    "evaluate:samples": "npm run evaluate:samples --prefix backend"
  }
}
